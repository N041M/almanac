// @almanac/meals — the meal-selection engine (§6). The first major module
// bolted onto the calendar core. Depends on @almanac/core and @almanac/food
// only — never another module (L1). Phase 4 implements the engine exactly:
// gates, scorers, draw/generateWeek/rerollDay/commitWeek + the relaxation ladder.

export const MEALS_MODULE_VERSION = '0.0.0';
